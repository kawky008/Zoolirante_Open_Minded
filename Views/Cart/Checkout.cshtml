@model Zoolirante_Open_Minded.ViewModels.CartVM
@using Microsoft.AspNetCore.Mvc.Rendering
@inject IHttpContextAccessor HttpContextAccessor
@{
    ViewData["Title"] = "Checkout";
    var options = ViewBag.PickupOptions as IEnumerable<SelectListItem> ?? Enumerable.Empty<SelectListItem>();
}

<h2>Checkout</h2>

@if (TempData["CartMessage"] is string msg)
{
    <div class="alert alert-success">@msg</div>
}

<div class="card mb-3">
    <div class="card-body">
        <h5 class="card-title">Pickup Location</h5>

        <form asp-action="SetPickup" method="post" class="row gy-2 gx-2 align-items-center">
            @Html.AntiForgeryToken()
            <div class="col-auto">
                <label for="pickupSelect" class="col-form-label">Choose a location:</label>
            </div>
            <div class="col-auto">
                <select id="pickupSelect" name="id" class="form-select">
                    @foreach (var opt in options)
                    {
                        var selected = (Model.PickupLocationId?.ToString() == opt.Value) ? "selected" : null;
                        <option value="@opt.Value" selected="@selected">@opt.Text</option>
                    }
                </select>
            </div>
            <div class="col-auto">
                <button type="submit" class="btn btn-primary">Save pickup</button>
            </div>
        </form>

        @if (!string.IsNullOrEmpty(Model.PickupLocationName))
        {
            <div class="mt-2 text-success">Selected: <strong>@Model.PickupLocationName</strong></div>
        }
    </div>
</div>

<h5>Order Summary</h5>
<table class="table table-sm">
    <thead>
        <tr><th>Item</th><th class="text-end">Price</th><th class="text-center">Qty</th><th class="text-end">Total</th></tr>
    </thead>
    <tbody>
        @foreach (var it in Model.Items)
        {
            <tr>
                <td>@it.Name</td>
                <td class="text-end">@it.Price.ToString("C")</td>
                <td class="text-center">@it.Qty</td>
                <td class="text-end">@it.LineTotal.ToString("C")</td>
            </tr>
        }
    </tbody>
    <tfoot>
        <tr>
            <th colspan="3" class="text-end">Subtotal</th>
            <th class="text-end">@Model.Subtotal.ToString("C")</th>
        </tr>
    </tfoot>
</table>

<div class="d-flex gap-2">
    <a class="btn btn-outline-secondary" asp-action="Index">Back to Cart</a>
    @* Placeholder submit; payment is out of scope *@

    @if (Context.Session.GetInt32("UserId") != null)
    {
        <form asp-controller="Payments" asp-action="Card" method="post" class="d-inline">
            @Html.AntiForgeryToken()
            <button class="btn btn-success" type="submit">Checkout</button>
        </form>
    }
    else{
        <button class="btn btn-success">PLease login to purchase merchandise</button>
    }
</div>

